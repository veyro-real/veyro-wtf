{
  "name": "veyro-wtf",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.4",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "deploy": "astro build && wrangler deploy",
    "deploy:status": "astro build && wrangler deploy -c wrangler.status.jsonc",
    "deploy:all": "astro build && wrangler deploy && wrangler deploy -c wrangler.status.jsonc"
  },
  "devDependencies": {
    "astro": "^5.16.2",
    "wrangler": "^4.61.0"
  }
}
